import React from 'react'
import BandsBrowser from './BandsBrowser'

/* Now Content only invokes one component. It can be easily extended if it's necessary */
const Content = () => {
  return <BandsBrowser />
}

export default Content
